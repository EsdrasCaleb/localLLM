package com.hf.sfm.util;

import org.mockito.*;
import org.junit.jupiter.api.*;
import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;
import javax.servlet.http.HttpSession;

@ExtendWith(MockitoExtension.class)
public class DataSource_getGridData_1_4_Test {

    // Test class
    @Test
    public void testGetGridData() {
        // BasePara basePara = new BasePara();
        // basePara.setSqlpath("sqlpath");
        // basePara.setPaging(true);
        // basePara.setStart(1);
        // basePara.setLimit(10);
        // basePara.setQueryValue("queryValue");
        // basePara.setSort("sort");
        // basePara.setDir("dir");
        // basePara.setQuerySql("querySql");
        // DataSource dataSource = new DataSource();
        // ListRange listRange = dataSource.getGridData(basePara);
        // Assert.assertEquals(listRange.getTotalSize(), 10);
        // Assert.assertEquals(listRange.getData().get(0).get(0), "queryValue");
    }
}
